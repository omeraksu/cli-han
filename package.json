{
  "name": "han",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@9.15.4",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "dev:hub": "pnpm --filter @han/hub dev",
    "dev:streamer": "pnpm --filter @han/runtime dev:streamer",
    "dev:viewer": "pnpm --filter @han/runtime dev:viewer",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
